# KC v0.6.0 Release Validation

## Scope

Release `@sawadari/kc@0.6.0` so npm, GitHub Action tags, GitHub Release, and KC evidence include the merged post-v0.5.0 improvements.

## Pre-Merge Verification

- `npm.cmd run check`: passed.
- `npm.cmd test`: passed, 44 tests.
- `npm.cmd run pack:dry`: passed, produced dry-run package `sawadari-kc-0.6.0.tgz`.
- `node lib/cli/index.js check --workspace . --mode pr ...`: passed.
- `node lib/cli/index.js check --workspace . --mode current ...`: passed for the active release artifact state.
- `git diff --check`: passed with line-ending warnings only.
- `dist/action/index.js`: regenerated by the build step; no committed content
  diff remained after index refresh.

## Post-Merge Validation

- PR #62 merged into main at `ab74a2235c7e82bfd38b964ec79414e2162f04fe`.
- main CI passed: https://github.com/sawadari/KC/actions/runs/25710523925
- `npm.cmd whoami`: blocked with `E401 Unauthorized` in the local environment.

Pending until npm authentication is restored:

- `npm.cmd publish --access public`
- `npm.cmd view @sawadari/kc version`
- `v0.6.0` tag creation
- `v0` tag move
- GitHub Release `v0.6.0`
- KC finalize

## Notes

- Issue: https://github.com/sawadari/KC/issues/61
- Approval: https://github.com/sawadari/KC/issues/61#issuecomment-4426938429
- Verification passed does not imply validation passed. npm, tag, and release checks must be recorded after merge.
